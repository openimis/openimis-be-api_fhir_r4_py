FHIR R4 Insurance
=================

Standalone Django app exposing InsurancePlan endpoints.

Install (editable):

```bash
pip install -e /Users/vijaykv/Desktop/neww/openimis-be-api_fhir_r4_py/fhir_R4_Insurance
```

Then add `fhir_R4_Insurance` to `INSTALLED_APPS` and include its URLs.


